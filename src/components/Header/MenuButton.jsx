import React from "react";
import { IoMenu } from "react-icons/io5";

const MenuButton = () => {
  return (
    <label htmlFor="my-drawer-3" className="drawer-button">
      <IoMenu />
    </label>
  );
};

export default MenuButton;
